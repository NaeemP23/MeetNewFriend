language: node_js
node_js:
  - stable
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build
before_install:
  - cd src
after_success:
  - firebase deploy --token $FIREBASE_TOKEN --non-interactive
